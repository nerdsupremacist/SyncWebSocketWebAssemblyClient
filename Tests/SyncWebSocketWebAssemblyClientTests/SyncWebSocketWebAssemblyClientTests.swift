import XCTest
@testable import SyncWebSocketWebAssemblyClient

final class SyncWebSocketWebAssemblyClientTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SyncWebSocketWebAssemblyClient().text, "Hello, World!")
    }
}
